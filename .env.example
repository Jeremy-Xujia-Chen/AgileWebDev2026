# Copy to .env for local development (never commit .env).
SECRET_KEY=change-me-to-a-long-random-string

# Reserved for AI Planner (teammate implements); unused while stub is active.
# PLANNER_API_KEY=
