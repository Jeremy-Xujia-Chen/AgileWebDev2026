[pytest]
pythonpath = .
testpaths = tests
markers =
    e2e: Selenium browser end-to-end tests (need Chrome)
# Flask-Login still uses datetime.utcnow(); noisy on Python 3.13+ until upstream fixes.
filterwarnings =
    ignore:.*utcnow.*:DeprecationWarning:flask_login\..*
