# StudySync — align with unit project stack (Flask + SQLAlchemy + tests).
Flask>=3.0,<4
Flask-SQLAlchemy>=3.1,<4
Flask-Login>=0.6.3,<1
Flask-WTF>=1.2,<2
WTForms>=3.1,<4
email-validator>=2.0,<3
python-dotenv>=1.0,<2
pytest>=8.0,<9
pytest-flask>=1.3,<2

# Selenium for checkpoint rubric (install browser driver separately).
selenium>=4.15,<5
